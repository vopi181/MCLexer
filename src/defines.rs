mod defines {
    #[derive(Debug)]
    pub enum cells_defines {
       variable,
       function,
    }
    pub #[derive(Debug)]
    struct cells {
        cell_type: cells_defines
        data: string
    }
}